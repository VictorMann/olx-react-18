export type ErrorType = {
  error?: string;
};

export type LoginType = {
  token?: string,
};

export type UFType = {
  id?: string;
  name?: string;
};

export type CategoryType = {
  id: number;
  description: string;
  image: string;
};